// export defaultを設定するとこのファイルに不空数関数があった場合でも
// これを設定した関数が代表として出力される
export default function Mycomponent() {
  return (
    <div>
        <p>これはMycomponentから出力している文字列です</p>
    </div>
  )
}

// export default Mycomponent;
